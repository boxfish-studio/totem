set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/Release")
file(GLOB source "src/*.c")
add_library(hal STATIC ${source})
target_include_directories(hal PUBLIC inc/)
target_link_libraries(hal pcbVersion Device FreeRTOS emlib dma usbLib _stdio xmodem)
