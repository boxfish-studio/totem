set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/Release")
file(GLOB source "src/*.c")
add_library(_stdio STATIC ${source})
target_include_directories(_stdio PUBLIC inc/)
target_link_libraries(_stdio Device)