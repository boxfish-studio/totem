set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/Release")
file(GLOB source "src/*.c")
add_library(dma STATIC ${source})
target_include_directories(dma PUBLIC inc/)
target_link_libraries(dma Device emlib)
