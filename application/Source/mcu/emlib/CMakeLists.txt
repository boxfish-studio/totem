set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/Release")
file(GLOB source "src/*.c")
add_library(emlib STATIC ${source})
target_include_directories(emlib PUBLIC inc/ ${CMAKE_SOURCE_DIR}/Source/totem/lib/usb/)
target_link_libraries(emlib Device)