set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/Release")
file(GLOB source "src/*.c")
add_library(usb STATIC ${source})
target_include_directories(usb PUBLIC inc/)
target_link_libraries(usb Device emlib)